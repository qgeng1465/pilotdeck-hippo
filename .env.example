# PilotDeck-Hippo 环境变量示例（复制为 .env 或写入环境）

# 端侧语义检索（可选）：设置后评分的 sim 项从 BM25 切换为本地 embedding cosine。
# 需要 npm i @huggingface/transformers；模型加载按路径缓存（同进程只加载一次），
# 单次加载超过 15s 视为失败并回退 BM25，不报错、不阻塞压缩。
# 未设置时直接使用 BM25，不报错、不崩溃。
# PILOTDECK_BGE_MODEL=Xenova/bge-small-zh-v1.5

# 真 LLM 评测（pnpm benchmark:real-llm）只从环境变量读密钥，不读磁盘文件：
#   PILOTDECK_EVAL_KEY（或 DEEPSEEK_API_KEY）必填，缺了会直接报错；
#   PILOTDECK_EVAL_URL 可选，指向任意 OpenAI 兼容端点（默认官方
#   https://api.deepseek.com/v1）。
# PILOTDECK_EVAL_URL=https://api.deepseek.com/v1
# PILOTDECK_EVAL_KEY=sk-xxx
# 评测模型（默认 deepseek-v4-flash）
# PILOTDECK_EVAL_MODEL=deepseek-v4-flash
